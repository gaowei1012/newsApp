import React from 'react'
import { Switch, Route, Router } from 'react-router-dom'

export default class NewsRouters extends React.Component {
    render() {
        return (
            <div>
                <Switch>
                    // 路由

                </Switch>
            </div>
        );
    }
}

